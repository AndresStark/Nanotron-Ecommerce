name: NanotronEcommerce
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - django==5.2.*
      - djangorestframework
      - django-cors-headers
      - psycopg[binary]
      - python-dotenv
      - gunicorn